<?xml version="1.0" encoding="UTF-8"?>
<!-- ============================================================
     pom.xml — Configuración Maven del proyecto Java
     Usa las librerías gRPC instaladas localmente vía apt,
     así funciona sin acceso a internet (para entornos cerrados).
     Para entornos con internet, descomente la sección "alternativa".
     ============================================================ -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <!-- ── Identidad del proyecto ── -->
  <groupId>calculadora.distribuida</groupId>
  <artifactId>grpc-calculadora</artifactId>
  <version>1.0.0</version>
  <packaging>jar</packaging>

  <properties>
    <java.version>11</java.version>
    <maven.compiler.source>${java.version}</maven.compiler.source>
    <maven.compiler.target>${java.version}</maven.compiler.target>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <!-- Versiones alineadas con grpc-java disponible en Ubuntu/Debian apt -->
    <grpc.version>1.41.3</grpc.version>
    <protobuf.version>3.21.12</protobuf.version>
  </properties>

  <dependencies>

    <!-- ── gRPC: Implementación con Netty como transporte HTTP/2 ── -->
    <dependency>
      <groupId>io.grpc</groupId>
      <artifactId>grpc-netty</artifactId>
      <version>${grpc.version}</version>
    </dependency>

    <!-- ── gRPC: Integración con Protobuf (serialización) ── -->
    <dependency>
      <groupId>io.grpc</groupId>
      <artifactId>grpc-protobuf</artifactId>
      <version>${grpc.version}</version>
    </dependency>

    <!-- ── gRPC: Stubs de cliente y servidor ── -->
    <dependency>
      <groupId>io.grpc</groupId>
      <artifactId>grpc-stub</artifactId>
      <version>${grpc.version}</version>
    </dependency>

    <!-- ── Protobuf: Librería de serialización de mensajes ── -->
    <dependency>
      <groupId>com.google.protobuf</groupId>
      <artifactId>protobuf-java</artifactId>
      <version>${protobuf.version}</version>
    </dependency>

    <!-- ── Anotaciones Java (requeridas por gRPC en Java 9+) ── -->
    <dependency>
      <groupId>javax.annotation</groupId>
      <artifactId>javax.annotation-api</artifactId>
      <version>1.3.2</version>
    </dependency>

  </dependencies>

  <build>
    <extensions>
      <!-- Detecta el SO para descargar el binario protoc correcto -->
      <extension>
        <groupId>kr.motd.maven</groupId>
        <artifactId>os-maven-plugin</artifactId>
        <version>1.7.1</version>
      </extension>
    </extensions>

    <plugins>

      <!-- ── Plugin protoc: Genera clases Java desde .proto ── -->
      <!-- Se ejecuta automáticamente con: mvn generate-sources  -->
      <plugin>
        <groupId>org.xolstice.maven.plugins</groupId>
        <artifactId>protobuf-maven-plugin</artifactId>
        <version>0.6.1</version>
        <configuration>
          <protocArtifact>
            com.google.protobuf:protoc:${protobuf.version}:exe:${os.detected.classifier}
          </protocArtifact>
          <pluginId>grpc-java</pluginId>
          <pluginArtifact>
            io.grpc:protoc-gen-grpc-java:${grpc.version}:exe:${os.detected.classifier}
          </pluginArtifact>
        </configuration>
        <executions>
          <execution>
            <goals>
              <goal>compile</goal>        <!-- Genera clases Protobuf -->
              <goal>compile-custom</goal> <!-- Genera stubs gRPC -->
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- ── Plugin shade: crea un fat JAR con todas las deps ── -->
      <!-- Resultado: target/grpc-calculadora-1.0.0.jar           -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <version>3.5.0</version>
        <executions>
          <execution>
            <phase>package</phase>
            <goals><goal>shade</goal></goals>
            <configuration>
              <createDependencyReducedPom>false</createDependencyReducedPom>
              <!-- Elimina firmas digitales que rompen los fat JARs -->
              <filters>
                <filter>
                  <artifact>*:*</artifact>
                  <excludes>
                    <exclude>META-INF/*.SF</exclude>
                    <exclude>META-INF/*.DSA</exclude>
                    <exclude>META-INF/*.RSA</exclude>
                  </excludes>
                </filter>
              </filters>
            </configuration>
          </execution>
        </executions>
      </plugin>

    </plugins>
  </build>

</project>
